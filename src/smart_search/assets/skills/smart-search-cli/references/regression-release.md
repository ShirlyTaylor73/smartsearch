# Regression and Release

Run compileall, the complete pytest suite, `npm test`, CLI help/version examples, and mock smoke. Verify repository and packaged skill directories are byte-identical.

Breaking prereleases use `0.3.0-beta.N` and npm dist-tag `next`. The package is `@shirlytaylor73/smart-search`; npm versions are immutable.
